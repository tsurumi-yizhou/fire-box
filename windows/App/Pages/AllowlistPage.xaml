<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="App.Pages.AllowlistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:services="using:App.Services"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,12"
            Orientation="Horizontal"
            Spacing="8">
            <TextBlock
                x:Uid="AllowlistTitle"
                VerticalAlignment="Center"
                Style="{ThemeResource SubtitleTextBlockStyle}" />
            <Button
                x:Uid="AllowlistRefresh"
                VerticalAlignment="Center"
                Click="Refresh_Click" />
            <ProgressRing
                Width="20"
                Height="20"
                VerticalAlignment="Center"
                IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />
        </StackPanel>

        <!-- List -->
        <ListView
            Grid.Row="1"
            ItemsSource="{x:Bind ViewModel.Entries}"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="services:AllowlistEntryDto">
                    <Grid Padding="0,8" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock
                                x:Uid="AllowlistColumnDisplayName"
                                Style="{ThemeResource CaptionTextBlockStyle}"
                                Opacity="0.6" />
                            <TextBlock Text="{x:Bind DisplayName}" TextTrimming="CharacterEllipsis" />
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock
                                x:Uid="AllowlistColumnAppPath"
                                Style="{ThemeResource CaptionTextBlockStyle}"
                                Opacity="0.6" />
                            <TextBlock Text="{x:Bind AppPath}" TextTrimming="CharacterEllipsis" />
                        </StackPanel>

                        <Button
                            x:Uid="AllowlistRevoke"
                            Grid.Column="2"
                            VerticalAlignment="Center"
                            Click="Revoke_Click"
                            Tag="{x:Bind AppPath}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
